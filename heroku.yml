build:
  docker:
    web: Dockerfile
    worker:
       dockerfile: Dockerfile
       target: builder
release:
  image: worker
  command:
    - echo Launched something (probably)
# build:
#   docker:
#     web: frontend/Dockerfile
#     worker: backend/Dockerfile 
