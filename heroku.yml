build:
  docker:
    worker: backend/Dockerfile
    web: frontend/Dockerfile

# release:
#   image: web
#   command:
#     - echo 'Starting go server after deployment!';
run: 
  worker: ./go
  web: npm start